apiVersion: v1
kind: Service
metadata:
  name: ativa-idoso-api-tcp
  namespace: analysis
  labels:
    app: ativa-idoso-api-tcp
spec:
  selector:
    app: api
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
